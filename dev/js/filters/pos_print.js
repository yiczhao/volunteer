export default  (value) => {
    if(!!value){
        value=value.replace(new RegExp(/(DISRATE)/g),'用户折扣')
        value=value.replace(new RegExp(/(DISRATE)/g),'用户折扣')
        value=value.replace(new RegExp(/(DISRATE)/g),'用户折扣')
        value=value.replace(new RegExp(/(DISRATE)/g),'用户折扣')
	}
	return value;
}
